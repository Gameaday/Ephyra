# Gradle
.gradle/
.kotlin/
build/
*.pyc

# IDE - IntelliJ / Android Studio
*.iml
.idea/
!.idea/icon.svg
/captures/
/.externalNativeBuild/
/.cxx/

# Configuration
local.properties

# Telemetry/Firebase build inputs (only needed with -Pinclude-telemetry)
google-services.json

# Logs
*.log

# JVM heap dumps (from -XX:+HeapDumpOnOutOfMemoryError)
*.hprof

# Build output scratch logs
build_output.txt
build_assemble_output.txt
build-log*.txt
gradle_domain*.txt
gradle_domain*.txt.err

# Local helper scripts
_*.ps1

# OS
.DS_Store
Thumbs.db

# Agent Manager worktrees. Each `.kilo/worktrees/<name>/` is a full second copy of the
# repository, so leaving it untracked-by-accident puts ~1465 duplicate sources in reach of
# `git add .`. Size metrics in doc/BUILD_HEALTH.md must exclude this path for the same reason.
.kilo/

# Project IDX
# .idx/dev.nix (Keep tracked)
*.pytroubleshooting/
